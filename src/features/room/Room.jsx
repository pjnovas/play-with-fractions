import React from 'react';
// import { useWebSocket } from './websockets';

const Room = () => (
  // const [{ status }, { setNickname }] = useWebSocket();
  <div className="game">Room</div>
);

export default Room;
