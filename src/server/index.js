//  import util from 'util';
import createStore from './createStore';

const store = createStore();

// console.log(util.inspect(store.getState(), false, null, true));

// store.dispatch({ type: 'TEST_WS' });
